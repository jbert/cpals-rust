DONE - adding .normalise() to char_freq() method changed results, so:
  - either normalise() is wrong
  - or the total was wrong

DONE - remove CharCount, can simply use char frequencies with a call to normalise

- change distance to:
  - sort the letters into order
  - find some distance on ordering
    - number of swaps to make first N match?
    - 'earth movers' to make first N match?
  - re-run without noise chars
